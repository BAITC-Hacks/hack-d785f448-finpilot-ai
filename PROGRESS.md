# Проверка интеграции — 23 сентября 2026

Docker восстановлен после освобождения места и перезапуска WSL. Прежние ошибки read-only file system устранены. Engine 29.8.0, Compose 5.5.1. На опубликованной основе main 1e56e56 использованы готовые компоненты команды; новая реализация ассистента не создавалась.

## Реально проверено через Docker

- docker compose up --build: приложение и PostgreSQL работают; оба контейнера healthy. Порт приложения 127.0.0.1:8000, порт БД наружу не опубликован.
- /api/health: ok=true, db.ok=true, mode=replay, 2248 узлов. В базе шесть таблиц: runs, nodes_roles, node_flags, block_plan, agent_runs, approvals.
- Пайплайн из parquet запускается внутри контейнера; check.py --prev out_sample: 28 проверок, 0 ошибок, 0 пропусков.
- POST /api/agent вызывает готовый assistant.py; полные факты, ответ и run_id записываются в PostgreSQL.
- POST /api/approve сохраняет подтверждение и отказ. Четыре одновременных повтора дают один approval_id и одну запись действия в agent_runs. Противоположное решение/устаревший черновик дают 409. Старый GET confirm работает без дубликатов.
- При остановленной PostgreSQL health, explain и confirm возвращают 503. Ошибка записи больше не скрывается.
- После docker compose down и up -d сохранились все счётчики: runs=1, nodes_roles=2248, node_flags=2248, block_plan=30, agent_runs=4, approvals=2. Последующие объяснения увеличивают журнал.
- Повторный db_load.py пропускает уже загруженный run_id 2f2d24fb298eef28, дубликатов набора нет.
- В браузере открыты демо-проект, очередь 30, поиск GID 100000002398779100, карточка, объяснение с явными replay/template и черновик запроса. Скрипты карты и приложения загружены с 127.0.0.1:8000; CDN-скрипта в DOM нет. Отдельная трассировка всех сетевых запросов ещё не проводилась.

## Что исправлено интегратором

Ошибки БД возвращаются клиенту; решения идемпотентны и связаны с результатом расчёта. Добавлены согласованные POST-маршруты через адаптер существующих сцен. В фактах и инструкции явно указано seed → … → выбранный клиент. Вызов OpenAI Responses API содержит store=false. Подмена ошибки модели шаблоном удалена: HTTP 502 с явной ошибкой, таймаут SDK 45 секунд без автоматических повторов. Отдельный тест успешно имитировал сбой SDK без возврата replay; это не live-тест. Сохранены шесть таблиц, формулы, пороги и веса. web/ не редактировался.

## Что остаётся проверить или передать

- Чистый клон main 549921f получен с GitHub в finpilot-compose-clean. Новый проект Compose finpilot-acceptance, новый пустой том PostgreSQL, .env только из примера: сборка и запуск успешны; health зелёный, повторно прошли 28/28 проверок и check_integration.py.
- На коде main 845ee5f два live-вызова выполнены через кнопку «Объяснить» сайта и реальный API на gpt-6-luna. PostgreSQL: agent_runs.id=5 и 6, mode=live, _source=live:gpt-6-luna. Для GID 100000002398779100 текст верно описывает 8 других seed с путями к клиенту; для 100000003684369100 — 9. Направление в обоих ответах корректное. GPT-5.6 Sol в этом прогоне не использовалась.
- Юрию: текущая кнопка «Отклонить» только скрывает черновик. Подключить готовый POST /api/approve с approved=false по API_CONTRACT.md. Backend отказ уже пишет и проверен.
- Демо не удостоверяет личность аналитика; reviewer=analyst. Подтверждение сохраняет локальный файл, не отправляет запрос наружу. Режим whatif оставлен из готового ассистента; отдельная сверка всех его сценариев с полным перерасчётом не проводилась.

Рабочие инструкции: README.md, DOCKER.md, API_CONTRACT.md. Проверка API и БД воспроизводится check_integration.py.

## Финальная проверка кода 845ee5f

28/28 математических проверок повторно прошли. Полный check_integration.py также прошёл на этой версии: временный API-процесс внутри контейнера был явно запущен без OpenAI-ключа на непубликуемом порту 8001 и затем остановлен; основной сайт на 8000 остался в live. Счётчики после проверки: 1 / 2248 / 2248 / 30 / 8 / 2 соответственно шести таблицам.

Фактический рабочий Compose сейчас — finpilot-acceptance из чистого клона finpilot-compose-clean. Ключ находится в его исключённом из Git .env и передан контейнеру; проверялась только его непустота, значение не выводилось. В исходном finpilot-upload/.env ключ по-прежнему пуст. На /api/health: ok=true, db.ok=true, mode=live, model=gpt-6-luna. Последующие изменения отчёта не меняют проверенный код.

Ограничение live-качества: второй ответ неточно написал «входящие переводы от seed, находящихся вне выборки». Исходный факт означает, что у выбранного seed не выгружены входящие из-за границ выборки; тип отправителей неизвестен. Это не ошибка направления seed_reach, но весь текст нельзя считать полностью проверенным. Обучение/дообучение модели не выполнялось.

Точная реализация replay: без ключа Model.ask читает fixtures/replay.json, если файл есть; при отсутствии записи использует подготовленные шаблоны template_answer на фактах. В текущем репозитории файла fixtures/replay.json нет, поэтому проверен именно шаблонный replay, не воспроизведение записанных live-ответов.
